package org.deviceservice.app.utility;

public enum Codes {

    OK,
    USER_EXIST,
    USER_NOT_FOUND,
    INVALID_CREDENTIALS,
    PASSWORD_CHANGE_ERROR,
    INCORRECT_TOKEN,
    VALIDATION_ERROR
}
